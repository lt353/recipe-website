<!-- 
    This header includes:
    - A site title that links back to the homepage.
    - A navigation bar that dynamically updates:
      - On the homepage, no links appear (since they are already in the body).
      - On recipe pages, links to Home and other recipes (except the current one) appear.
    - The recipe titles are correctly encoded to avoid issues with spaces.
-->

<header>
    <h1>Lindsay's Recipe Collection</h1>

    <nav>
        <% if (typeof currentPage !== "undefined" && currentPage !== "/") { %> 
            <a href="/">Home</a> <!-- Always include Home link on recipe pages -->

            <% if (typeof recipes !== "undefined") { %>
                <% recipes.forEach((recipe) => { %>
                    <% if (recipe.title !== currentPage) { %>
                        <!-- Only show links to OTHER recipes, not the one currently viewed -->
                        | <a href="/recipe/<%= encodeURIComponent(recipe.title) %>"><%= recipe.title %></a>
                    <% } %>
                <% }); %>
            <% } %>
        <% } else { %>
            <span class="nav-placeholder"><a href="#recipes">Browse Through My Recipes Below!</a></span>
        <% } %>
    </nav>
</header>
